{
  "name": "adutha-stoppil-gameplay-systems",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node dist/tests/run-tests.js",
    "simulate": "npm run build && node dist/harness/simulate.js",
    "check": "npm run test && npm run simulate"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "~6.0.2"
  }
}
